
技巧1 结识.命令
.命令可以让我们重复上次的修改，它是Vim中最为强大的多面手
"上次的修改"可以指很多东西，一次修改的单位可以是字符，整行，甚至是整个文件

我们使用../the_vim_way/0_machanics.txt文本进行说明:

x命令会删除光标下的字符，这种情况下使用.命令“重复上次修改”时，就会让Vim删除光标下的字符：

Line one
Line two
Line three
Line four

u命令可以撤销修改，使文档恢复到初始状态


dd命令也做删除操作--删除整行,dd后使用.命令，那么“重复上次修改”会让Vim删除当前行

Line one
Line two
Line three
Line four


>G命令会增加从当前行，到文档末尾处吃的缩进层级。在这个命令之后使用.命令，那么“重复上次修改”会让Vim增加从当前行到文档末尾的锁进层级
Line one
    Line two
        Line three
            Line four

h左移一个字符
j下移一个字符
k上移一个字符
l右移一个字符

x,dd,>以及移动光标命令都是在普通模式中执行的命令，我们每次进入插入模式，也会形成一次修改。从修改插入模式的那一刻起(例如,输入i)，知道返回普通模式为止(输入<Esc>),Vim会记录每一个按键操作。做出这样一个修改后再用.命令的话，它将会重新执行所有这些按键操作

.命令是一个微型的宏
Vim可以录制任意数目的按键操作，然后在以后重复执行它们。这让我们可以把最常重复的工作流程录制下来，并用一个按键重放它们。我们可以把.命令当成一个很小的宏(macro)
