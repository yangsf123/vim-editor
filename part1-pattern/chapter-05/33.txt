技巧33   把当前单词插入到命令行


即使是在命令行模式下，Vim也始终知道光标位于何处以及哪个分割窗口处于活动状态。为节省时间，我们可以把活动窗口中的当前单词(或字符串)插入到命令行中.


在Vim的命令行下，<C-r><C-w>映射项会复制光标下的单词并把它插入到命令行中。我们可以利用这一功能减少击键次数

假设我们想把下面这段代码中的变量tally重命名为counter:

var tally;
for(counter=1;tally<=10;tally++) {
    // do something with tally
};
